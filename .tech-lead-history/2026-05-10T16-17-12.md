# Tech Lead отчёт — 2026-05-10T16-17-12

## Резюме
Запуск инициирован UI-Kit Compliance анализом из consumer-проекта `tg_parser` (Vue 3 + ant-design-vue). Kit написан на React 19 + SCSS modules — стек несовместим с Vue-консьюмером. Ниже зеркалируются задачи из `tg_parser/.uikit-compliance-history/2026-05-10T16-17-12.md`, относящиеся к UI-киту, чтобы они появились в фиде тех-лида самого ui-kit'а.

## Источник
- Консьюмер: `/Users/alicia/Desktop/xopa/tg_parser/`
- Полный отчёт: `tg_parser/.uikit-compliance-history/2026-05-10T16-17-12.md`

## Предлагаемые задачи

### [Tech Debt] Стратегия совместимости UI-кита и Vue-консьюмеров
- Что нарушено: UI-кит написан на React 19, минимум один консьюмер (`tg_parser/frontend`) — на Vue 3. Использовать кит из Vue невозможно, единой стратегии нет.
- Как исправить: принять решение — отдельный пакет `ui-kit-vue` (порт), Web Components обёртка (Lit / `@r2wc/react-to-web-component`) или официальный отказ от поддержки Vue.
- Специалист: fullstack
- Сложность: complex
- Срочность: высокая

**Анализ:**
Это блокер для всех остальных задач. В `src/shared/ui/` лежат `Button.tsx`, `Input.tsx`, `InputPassword.tsx`, `Typography.tsx`, `EyeToggle/index.tsx` — React-only. В Vue-консьюмерах их нечем заменить. Без выбранной стратегии любые задачи на Vue-порты лишены адресата (куда писать код), а consumer-задачи на замену лишены целевого артефакта импорта. Решение влияет на структуру репо (моно-пакет vs монорепо), pipeline storybook и стратегию релизов.

**План:**
1. Собрать сравнение трёх вариантов (порт / web components / отказ) с оценкой стоимости и поддержки.
2. Согласовать с продуктом и фронтенд-лидами консьюмеров.
3. Зафиксировать решение в `ui-kit/README.md` и каскадно — в `TECH-LEAD.md` консьюмеров.
4. Создать пакет / каркас под выбранный вариант (если не «отказ»).

**Ключевые файлы:**
- `package.json` — точка решения о структуре пакета
- `src/shared/ui/*` — исходники для портирования
- `README.md` — место для итогового решения

### [Tech Debt] Vue-порт компонента Button
- Что нарушено: `Button` экспортируется только как React-компонент (`src/shared/ui/Button/Button.tsx`). Vue-консьюмер `tg_parser` использует `a-button` в `SearchForm.vue`, `ResultsList.vue`, `App.vue` — заменить нечем.
- Как исправить: реализовать Vue-эквивалент Button с тем же API (variants, size, loading, disabled), стили перенести из `button.module.scss`, обеспечить storybook-стори.
- Специалист: frontend
- Сложность: medium
- Срочность: высокая

**Анализ:**
Самый частый компонент в consumer'ах — кнопка. Без Vue-варианта Button невозможно начать миграцию Vue-проектов. Сохранить ту же типовую систему (`primary`, `secondary`, размеры), что и в React-варианте, чтобы не плодить отдельный визуальный язык. Параллельно решается под зонтиком стратегической задачи выше.

**План:**
1. Зависимость: после "Стратегия совместимости UI-кита и Vue-консьюмеров".
2. Скопировать API React-Button: пропсы, варианты, размеры, состояния.
3. Реализовать Vue 3 SFC + перенос `button.module.scss`.
4. Storybook-стори под все варианты.
5. Опубликовать пакет / экспорт.

**Ключевые файлы:**
- `src/shared/ui/Button/Button.tsx` — образец API
- `src/shared/ui/Button/button.module.scss` — стили
- `src/shared/ui/Button/Button.stories.tsx` — стори как референс

### [Tech Debt] Vue-порт компонентов Input и InputPassword (+ EyeToggle)
- Что нарушено: `Input`, `InputPassword`, `EyeToggle` существуют только в React (`src/shared/ui/Input/`, `src/shared/ui/EyeToggle/`). Vue-консьюмер использует `a-input`, `a-input-number`, `a-textarea`.
- Как исправить: Vue-версии Input и InputPassword с встроенным Vue-EyeToggle, сохранение API, перенос SCSS-модулей.
- Специалист: frontend
- Сложность: medium
- Срочность: высокая

**Анализ:**
Форма поиска в consumer (`tg_parser/frontend/src/pages/home/ui/SearchForm.vue`) — основное место ввода. Текстовые/цифровые кейсы должны закрываться `Input` из кита. `InputPassword` пока не используется в этом consumer'е, но логически парный — без него EyeToggle висит сиротой.

**План:**
1. Зависимость: после "Стратегия совместимости UI-кита и Vue-консьюмеров".
2. Описать API Vue-Input по образцу `Input.tsx` (label, error, prefix/suffix, sizes).
3. Перенести `input.module.scss`, `inputWrapper.module.scss`, `mixins.module.scss`.
4. Реализовать Vue-EyeToggle и Vue-InputPassword поверх Input.
5. Storybook-стори. Покрыть случаи number/textarea (или зафиксировать TODO, если за пределами scope).

**Ключевые файлы:**
- `src/shared/ui/Input/Input.tsx`, `InputPassword.tsx`, `InputWrapper.tsx`
- `src/shared/ui/Input/*.module.scss`
- `src/shared/ui/EyeToggle/index.tsx`

### [Tech Debt] Vue-порт компонента Typography
- Что нарушено: `Typography` в ките только React. У consumer `tg_parser` есть локальный `frontend/src/shared/ui/Typography.vue` — кандидат на замену, но цели для импорта нет.
- Как исправить: Vue-версия Typography с теми же вариантами/размерами и токенами.
- Специалист: frontend
- Сложность: simple
- Срочность: средняя

**Анализ:**
Локальный `Typography.vue` в consumer'е дублирует ответственность кита, но без Vue-варианта в ките его нечем заменить. Стори в ките есть (`Typography.stories.tsx`) — API уже зафиксирован. Логично сначала закрыть Button/Input, потом Typography, поскольку он реже встречается.

**План:**
1. Зависимость: после "Стратегия совместимости UI-кита и Vue-консьюмеров".
2. Скопировать API React-Typography (variant, weight, color).
3. Перенести `index.module.scss`.
4. Storybook-стори.

**Ключевые файлы:**
- `src/shared/ui/Typography/Typography.tsx`
- `src/shared/ui/Typography/index.module.scss`
